name: Temporary Featured Figma gap validation

on:
  push:
    branches:
      - fix/featured-carousel-figma-gap-structure

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - run: npm ci
      - name: Featured layout tests
        run: node --test tests/featuredProjectsGallery.test.js tests/featuredProjectsTouchNavigation.test.js tests/featuredMobileAxisOwnership.test.js
      - name: Regression suite excluding known Contact baseline
        run: node --test $(find tests -name '*.test.js' ! -name 'contactSection.test.js' | sort)
      - run: npm run build
