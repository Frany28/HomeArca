name: Temporary iOS carousel autoplay validation

on:
  push:
    branches:
      - fix/ios-featured-carousel-autoplay

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - run: npm ci
      - name: Featured carousel tests
        run: node --test tests/featuredMobileAxisOwnership.test.js tests/featuredProjectsTouchNavigation.test.js tests/featuredProjectsGallery.test.js tests/crossBrowserGestureOwnership.test.js
      - name: Regression suite excluding known Contact baseline
        run: node --test $(find tests -name '*.test.js' ! -name 'contactSection.test.js' | sort)
      - run: npm run build
